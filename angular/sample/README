
## A simple AngularJS Sample Book Store example

Create the gitana.json file for your Cloud CMS project in server folder by either using the cloudcms-cli or
download from "API Keys" under "Manage Project" in the Cloud CMS Project.

Build the app.

Final compiled files will be copied to server/public:

    npm install
    ng build --prod

Start the server:
    cd server
    npm install
    npm Start

Open in your browser:
    http://localhost:2999

